{
  "name": "medway-stamps",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "pages:dev": "concurrently -n build,pages -c blue,green \"npm:dev:build-watch\" \"npm:dev:pages\"",
    "dev:build-watch": "vite build --watch",
    "dev:pages": "wrangler pages dev dist --live-reload",
    "typecheck": "tsc --noEmit",
    "typecheck:functions": "tsc --noEmit -p functions/tsconfig.json",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply medway-stamps-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply medway-stamps-db --remote",
    "db:seed:local": "wrangler d1 execute medway-stamps-db --local --file=./db/seed.sql",
    "db:seed:remote": "wrangler d1 execute medway-stamps-db --remote --file=./db/seed.sql"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "hono": "^4.6.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.0",
    "drizzle-kit": "^0.28.1",
    "sass": "^1.103.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "wrangler": "^3.91.0"
  }
}
