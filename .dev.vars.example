# Copy to .dev.vars (gitignored) and fill in real values for local dev.
# Get test-mode keys from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_...
# From `stripe listen --print-secret` (Stripe CLI) for local webhook testing
STRIPE_WEBHOOK_SECRET=whsec_...
