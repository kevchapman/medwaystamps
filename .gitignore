node_modules/
dist/
.wrangler/
.dev.vars
.env
.env.local
*.log
.DS_Store
.claude/

# Local image cache from `npm run db:pull:remote` (scripts/pull-remote-data.mjs)
.data/

# Design canvas: keep the .dc.html sources + canvas.json, skip the
# generated seeded payload (large, reproducible via the design skill).
design/*-direction.html
